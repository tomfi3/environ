services:
  - type: web
    name: environ-dashboard
    runtime: python
    pythonVersion: 3.12.3   # <— must be here, correctly indented
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app_modern:app.server
