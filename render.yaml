services:
  - type: web
    name: airdash
    runtime: python
    pythonVersion: 3.12
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: gunicorn app:server
